// Used to split bundles in production builds
// Modules imported here are bundled into vendor.js
// Everything else is bundled into main.js
// Uses the webpack.optimize.CommonsChunkPlugin

/* eslint-disable no-unused-vars */

import fetch from 'whatwg-fetch';
